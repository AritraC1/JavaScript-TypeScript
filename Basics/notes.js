"use strict"; // treat all js code as newer version (cannot be undone). It is already in use in the current js versions

// alert(" ALERT!! ") // works in browser, In JS, there is a different syntax for using alert.